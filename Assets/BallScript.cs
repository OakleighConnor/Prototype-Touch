using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UIElements;

public class BallScript : MonoBehaviour
{
    /*
    TouchScript Touch = new TouchScript();
    public GameObject barrier;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        if (inRange == true)
        {
            print("TRUE!!!!!!!!");
            // Position the object.
            //JoyStickBall.transform.position = position;
            transform.position = Vector3.MoveTowards(transform.position, Touch.position, 100 * Time.deltaTime);
        }
        else
        {

        }
    }
    /*
    private void OnColliderExit2D(Collision2D barrier)
    {
   

        print("FALSE!!!!!!!!!!!!!");
        Touch.inRange = false;
    }
    */
}
